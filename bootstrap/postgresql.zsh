#!/bin/sh

# Postgres.app
curl https://github.com/PostgresApp/PostgresApp/releases/download/v2.0.2/Postgres-2.0.2.dmg $HOME

# sudo mkdir -p /etc/paths.d &&
# echo /Applications/Postgres.app/Contents/Versions/latest/bin | sudo tee /etc/paths.d/postgresapp
# Postico
curl https://eggerapps.at/postico/download/ $HOME
