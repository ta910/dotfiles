#### directories.zsh
# directories.zsh is aliases of cd command

source "$HOME/.zsh/src/aliases/directories/locona.zsh"
source "$HOME/.zsh/src/aliases/directories/3shake.zsh"

# default
#---------------------------------
alias -g ...='../..'
alias -g ....='../../..'
alias -g .....='../../../..'


alias -g ho="$HOME"
alias -g gopus="$HOME/gopus"
alias -g zdir="$HOME/.zsh/src"

GITHUB="$HOME/w/github.com"
alias -g crea="$GITHUB/creasty/"

